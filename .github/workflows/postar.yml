name: Postagem Automática - Mosaico na FATEC

on:
  schedule:
    # Roda a cada 15 minutos, todos os dias. O script decide se há algo
    # para postar naquele horário, consultando o schedule.json.
    # IMPORTANTE: horário do cron do GitHub é sempre em UTC.
    - cron: "*/15 * * * *"

  # Permite rodar manualmente pela aba "Actions" no GitHub, passando
  # um dia/hora específico para testar sem esperar o horário real.
  workflow_dispatch:
    inputs:
      dia:
        description: "Dia da semana para teste (ex: segunda-feira)"
        required: false
        default: ""
      hora:
        description: "Horário para teste (ex: 08:00)"
        required: false
        default: ""

jobs:
  postar:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar dependências
        run: pip install -r requirements.txt

      - name: Rodar o script de postagem
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          INSTAGRAM_ACCESS_TOKEN: ${{ secrets.INSTAGRAM_ACCESS_TOKEN }}
          INSTAGRAM_BUSINESS_ID: ${{ secrets.INSTAGRAM_BUSINESS_ID }}
          DRIVE_FOLDER_ID: ${{ secrets.DRIVE_FOLDER_ID }}
          GOOGLE_SERVICE_ACCOUNT: ${{ secrets.GOOGLE_SERVICE_ACCOUNT }}
          CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_API_KEY: ${{ secrets.CLOUDINARY_API_KEY }}
          CLOUDINARY_API_SECRET: ${{ secrets.CLOUDINARY_API_SECRET }}
          DAY_OVERRIDE: ${{ github.event.inputs.dia }}
          HOUR_OVERRIDE: ${{ github.event.inputs.hora }}
        run: python main.py
